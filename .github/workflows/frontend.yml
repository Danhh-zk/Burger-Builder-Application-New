name: CI/CD - Frontend

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  frontend-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Install Dependencies (Frontend)
        run: npm install --prefix ./frontend

      - name: Build Frontend (React + Vite)
        run: npm run build --prefix ./frontend

      - name: Run Frontend Tests
        run: npm run test --prefix ./frontend

 
  frontend-cd:
    runs-on: ubuntu-latest
    needs: frontend-ci  # Ensure CD runs after CI
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build Frontend Docker Image
        run: docker build -t xdn73/frontend:latest -t xdn73/frontend:${{ github.sha }} ./frontend

      - name: Push Frontend Docker Image to Docker Hub
        run: |
          docker pull xdn73/frontend:latest

      - name: Deploy Frontend to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: g5-frontend-app  
          images: xdn73/frontend:latest
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE_FRONTEND }}

